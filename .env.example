# .env.example - copy to .env and fill in values as needed.
# The pipeline tries configured generation providers in config.json order.
# You can use Claude CLI, Anthropic API, Codex CLI, or deterministic fallbacks.

# ANTHROPIC_API_KEY=sk-ant-your-key-here
# SERPAPI_KEY=your-serpapi-key-here
